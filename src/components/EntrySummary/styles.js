import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    //flex: 1,
  },
  inner: {
    flexDirection: 'row',
    paddingVertical: 10,
  },
});

export default styles;
